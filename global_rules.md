# AI开发系统规则 v3.0

**适用项目**: 混合算力融合平台（Hybrid Compute Platform）  
**核心原则**: 根因修复，系统和架构功能优先，保持架构一致
**创建新文件时，AI必须提交申请，等审查员复核审批后才能进行。临时脚本和测试脚本除外**
**每次会话尾部，AI必须输出：程序员**
**每次生成文档按照下面的规范：
- 格式命名：日期+当日生成的文档的序号+文件名**
- 在文档的显著位置标记上“程序员”，和 windsurf 的标记，生成日期，时间
- 在文档的开头，写上概述，以及这个文档的产生场景和背景
- 你的点评，审查文档等报告，要显性的提及你审核对象的文件名，路径
- 如果要引用其他文件，必须说明文件的路径，文件名，文件内容



## 🚨 **强制架构合规性检查（项目技术底座）**

**代码生成前（尤其是新方法/脚本/模块）必须先参考**:
- D:\AI-Projects\projects_share_structure\混合算力融合平台-gpt5-需求文档（固定版，当前唯一架构基线）
- 本规则内的“过渡门禁清单”（见下“P0 启动门槛（过渡门禁）”）
以便利用既有能力，避免重复造轮子。

**过渡期声明**：技术底座v1仍在设计中，尚未发布。在正式“技术底座契约”发布前，以“固定版需求文档+本规则过渡门禁”作为唯一门禁与基线；发布后，本规则将同步更新为以“底座契约”为主的门禁体系。

### **执行流程**
```
1. 对照固定版需求文档与本规则“过渡门禁清单”：
   - 队列（Job契约/幂等/ACK/退避/DLQ）
   - 对象存储预签名URL旁路
   - MCP工具接入约束
   - 观测与最小告警
   - 端口ACL与安全
2. 评估功能重叠度（目标<20%）与冗余度（<90%），产出ADR
3. 校验P0门槛（见文末）：未满足则禁止编码/合并
4. 确认SLO/成本口径影响与回滚策略
```

### **违规停止标准**
- 🚨 触发任一P0门槛缺失（数据不走对象存储旁路；无幂等/ACK/退避/DLQ；无最小观测/告警；SSH常态化；端口未收敛）- 立即停止
- 🔴 功能重叠>80% 或 冗余度>90% - 禁止继续  
- ⚠️ 超大提交（单PR>500行核心逻辑且无ADR/设计评审）- 可能重复实现
- 🔔 未对照底座契约与文档 - 违反约束

## 🤖 **Autogen与生态集成规范（可选兼容）**

### **推荐（选用时）**
- **版本**: Autogen 0.7.1
- **工具集成**: 优先使用Autogen内生工具机制暴露MCP能力
- **存储/注册**: 兼容既有UnifiedStorage与Registry（如采用）

### **项目硬性约束（无论是否采用Autogen）**
- 禁止绕过技术底座自建并行能力：
  - 绕过对象存储旁路（直接在MCP传大对象）
  - 绕过队列契约（无幂等/ACK/退避/DLQ）
  - 绕过安全与审计（SSH常态化、无轮换/留存）

## 🚫 **AI行为控制机制**

### **红绿灯决策机制**
- **🔴 红灯 - 禁止**: 任何P0门槛未满足；端口ACL未收敛；数据未旁路；无幂等/ACK/退避/DLQ；无最小观测/告警
- **🟡 黄灯 - 谨慎**: 新增文件>1个或代码>50行但已通过ADR与契约校验
- **🟢 绿灯 - 可以**: 契约齐备、P0达标、影响评估完备（SLO/成本/回滚）

### **危险行为监控**
```
❌ 立即警报: "直接开放公网端口" "直接在MCP传大文件" "先写完再补监控"
✅ 正确表述: "先对照底座契约检查..." "按对象存储旁路接入..." "按Job契约与模板实施..."
```

## 📏 **代码质量控制**

### **核心限制**
- **单文件（Python/JavaScript）**: 不得超过1000行（需拆分模块与模板）
- **代码重复**: 绝对不允许超过10%
- **根因修复**: 严格禁止症状修复
- **IaC/配置**: Compose/Ansible/YAML需通过lint与安全扫描

## 💻 **技术实施规范**

### **基本要求**
- **语言**: 所有交互使用中文
- **标记**: 所有报告冠以"程序员"标记
- **代码读取**: 优先使用直接方式，避免PowerShell
- **PowerShell**: 仅Windows语法，禁止Bash连接符

### **质量门禁**
- 任何变更必须通过“过渡门禁清单检查”（本规则P0小节：Job契约/数据旁路/观测与告警/安全与端口ACL）
- 新增功能必须在固定版需求文档的架构边界内实施，并通过契约测试与ADR评审
- 采用Autogen时需符合本节“可选兼容”要求

## 📌 **固定版基线与版本管理**
- 固定版需求文档为当前唯一架构基线，不得直接覆盖。
- 如需演进，请新增带日期的新版本文档并提交ADR评审，旧版保留存档。
- 待技术底座v1发布后，本规则将切换为以“底座契约”为主，固定版文档作为业务约束补充。

## 🧱 **P0 启动门槛（过渡门禁）**
- 数据通道：大对象统一走MinIO/S3预签名URL旁路，MCP仅轻载（建议<1MB）
- 作业编排：NATS/Redis Streams 幂等键、ACK、指数退避、DLQ、标准化Job状态机
- 观测与告警：Node/GPU/队列深度/端到端P95/失败率上线；最小告警阈值固化
- 安全与端口：SSH仅应急（MFA+JIT+审计+限源）；端口ACL白名单；证书/密钥轮换≤90天；审计留存≥180天
- 文档与治理：端口ACL实表入库；成本口径与SLO入库；ADR记录关键决策并定期复核

**AI必须成为"负责任的架构师"，而不是"代码生成器"**

请在每次交互的头部和尾部添加：“程序员”三个字；